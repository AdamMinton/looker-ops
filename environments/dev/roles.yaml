# 1. Permission Sets
# Triggering CI Test
permission_sets:
  - name: "Finance User Perms"
    permissions:
      - "access_data"
      - "see_looks"
      - "see_user_dashboards"
      - "explore"
  - name: "Finance User Perms 2"
    permissions:
      - "access_data"
      - "see_looks"
      - "see_user_dashboards"
      - "explore"

# 2. Model Sets
model_sets:
  - name: "Finance Models"
    models:
      - "thelook" # Using 'thelook' as it likely exists or is harmless model name for sample

# 3. Roles
roles:
  - name: "Finance Analyst"
    permission_set: "Finance User Perms"
    model_set: "Finance Models"
